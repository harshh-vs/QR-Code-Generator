<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>QR Code Generator</title>
<script src="https://cdn.jsdelivr.net/npm/qrcode-generator@1.4.4/qrcode.js"></script>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Syne:wght@700;800&family=DM+Mono:wght@400&display=swap');
  *{box-sizing:border-box;margin:0;padding:0}
  body{font-family:'Syne',system-ui,sans-serif;background:#0f0f0f;min-height:100vh;display:flex;align-items:center;justify-content:center;padding:2rem 1rem}
  .card{background:#1a1a1a;border-radius:20px;border:1px solid #2a2a2a;padding:2.5rem 2rem;width:100%;max-width:580px}
  .badge{display:inline-block;font-size:10px;font-weight:700;letter-spacing:.15em;text-transform:uppercase;color:#888;border:1px solid #333;border-radius:999px;padding:3px 12px;margin-bottom:1.2rem}
  h1{font-size:28px;font-weight:800;color:#fff;margin-bottom:6px;line-height:1.1}
  .sub{font-size:14px;color:#666;margin-bottom:2rem}
  .input-wrap{display:flex;gap:10px;margin-bottom:8px}
  input{flex:1;font-family:'DM Mono',monospace;font-size:13px;padding:0 16px;height:48px;border:1.5px solid #2e2e2e;border-radius:12px;background:#111;color:#e0e0e0;outline:none;transition:border-color .15s}
  input:focus{border-color:#555}
  input::placeholder{color:#444}
  button.gen{height:48px;padding:0 22px;font-family:'Syne',sans-serif;font-size:14px;font-weight:700;border:none;border-radius:12px;background:#fff;color:#111;cursor:pointer;white-space:nowrap;transition:opacity .15s,transform .1s}
  button.gen:hover{opacity:.88}
  button.gen:active{transform:scale(.97)}
  .err{color:#e74c3c;font-size:13px;display:none;margin-bottom:14px}
  .sizes{display:none;gap:8px;margin:16px 0;flex-wrap:wrap}
  .sz{font-size:12px;font-weight:700;padding:5px 16px;border:1.5px solid #2e2e2e;border-radius:999px;background:transparent;color:#666;cursor:pointer;transition:all .12s;font-family:'Syne',sans-serif}
  .sz.active,.sz:hover{background:#fff;color:#111;border-color:#fff}
  .result{display:none;margin-top:4px;gap:1.5rem;align-items:flex-start;flex-wrap:wrap}
  .result.show{display:flex}
  .qr-box{background:#fff;border-radius:14px;padding:14px;flex-shrink:0;display:flex;align-items:center;justify-content:center}
  #qr-canvas{display:block;border-radius:4px}
  .meta{flex:1;min-width:160px;display:flex;flex-direction:column;gap:1.1rem;padding-top:2px}
  .mlabel{font-size:10px;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:#555;margin-bottom:4px}
  .murl{font-family:'DM Mono',monospace;font-size:12px;color:#888;word-break:break-all;line-height:1.6}
  a.dl{display:inline-flex;align-items:center;gap:8px;height:40px;padding:0 18px;font-family:'Syne',sans-serif;font-size:13px;font-weight:700;border:1.5px solid #2e2e2e;border-radius:10px;background:transparent;color:#ccc;text-decoration:none;transition:all .12s}
  a.dl:hover{background:#222;color:#fff;border-color:#555}
  .tip{font-size:12px;color:#555;line-height:1.7;padding:10px 14px;border-left:2px solid #2a2a2a;background:#111;border-radius:0 8px 8px 0}
  .divider{border:none;border-top:1px solid #222;margin:2rem 0}
  .how{font-size:12px;color:#555;line-height:1.8}
  .how strong{color:#888;font-weight:700}
</style>
</head>
<body>
<div class="card">
  <div class="badge">Free Tool</div>
  <h1>QR Code Generator</h1>
  <p class="sub">Turn any link into a scannable QR code — free, instant, no sign-up.</p>

  <div class="input-wrap">
    <input id="inp" type="text" placeholder="https://yourlink.com" autocomplete="off" spellcheck="false"/>
    <button class="gen" onclick="generate()">Generate ↗</button>
  </div>
  <p class="err" id="err">Please enter a valid URL starting with http:// or https://</p>

  <div class="sizes" id="sizes">
    <button class="sz active" data-s="160" onclick="setSize(this)">Small</button>
    <button class="sz" data-s="240" onclick="setSize(this)">Medium</button>
    <button class="sz" data-s="320" onclick="setSize(this)">Large</button>
  </div>

  <div class="result" id="result">
    <div class="qr-box"><canvas id="qr-canvas"></canvas></div>
    <div class="meta">
      <div>
        <p class="mlabel">Link</p>
        <p class="murl" id="murl"></p>
      </div>
      <div>
        <p class="mlabel">Save</p>
        <a class="dl" id="dl" download="qrcode.png" href="#">⬇ Download PNG</a>
      </div>
      <p class="tip">Point any phone camera at the code — it opens the link instantly.</p>
    </div>
  </div>

  <hr class="divider"/>
  <p class="how"><strong>How to share this tool:</strong><br/>
  Host this HTML file on <strong>GitHub Pages</strong>, <strong>Netlify</strong>, or <strong>Vercel</strong> (all free) and share the link. Anyone with the link can use it in Chrome — no install needed.</p>
</div>

<script>
var curUrl = '', curSize = 160;

function isValid(u) {
  try { var x = new URL(u); return x.protocol === 'http:' || x.protocol === 'https:'; }
  catch(_) { return false; }
}

function drawQR(url, size) {
  var typeNumber = 0; // auto
  var errorCorrectionLevel = 'M';
  var qr = qrcode(typeNumber, errorCorrectionLevel);
  qr.addData(url);
  qr.make();

  var modules = qr.getModuleCount();
  var cv = document.getElementById('qr-canvas');
  cv.width = size;
  cv.height = size;
  var ctx = cv.getContext('2d');
  var cell = size / modules;

  ctx.fillStyle = '#ffffff';
  ctx.fillRect(0, 0, size, size);
  ctx.fillStyle = '#000000';

  for (var r = 0; r < modules; r++) {
    for (var c = 0; c < modules; c++) {
      if (qr.isDark(r, c)) {
        ctx.fillRect(
          Math.floor(c * cell),
          Math.floor(r * cell),
          Math.ceil(cell),
          Math.ceil(cell)
        );
      }
    }
  }
  document.getElementById('dl').href = cv.toDataURL('image/png');
}

function generate() {
  var url = document.getElementById('inp').value.trim();
  var err = document.getElementById('err');
  var res = document.getElementById('result');
  var szs = document.getElementById('sizes');

  if (!isValid(url)) {
    err.textContent = 'Please enter a valid URL starting with http:// or https://';
    err.style.display = 'block';
    res.classList.remove('show');
    szs.style.display = 'none';
    return;
  }

  err.style.display = 'none';
  curUrl = url;

  try {
    drawQR(url, curSize);
    document.getElementById('murl').textContent = url;
    res.classList.add('show');
    szs.style.display = 'flex';
  } catch(e) {
    err.textContent = 'URL is too long for QR. Try shortening it at bit.ly first.';
    err.style.display = 'block';
  }
}

function setSize(btn) {
  document.querySelectorAll('.sz').forEach(function(b){ b.classList.remove('active'); });
  btn.classList.add('active');
  curSize = parseInt(btn.dataset.s);
  if (curUrl) drawQR(curUrl, curSize);
}

document.getElementById('inp').addEventListener('keydown', function(e) {
  if (e.key === 'Enter') generate();
});
</script>
</body>
</html>
